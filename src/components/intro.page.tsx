import * as React from 'react';

export const IntroPage: React.SFC<any> = (props) => {
    return (
        <p className="App-intro">
            To get started, edit <code>src/App.tsx</code> and save to reload.
        </p>
    )
}

IntroPage.defaultProps = {

}
